{
  "name": "agent-rs-browser-example",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0 || >=22.12.0"
  },
  "scripts": {
    "prepare:wasm": "./build-wasm.sh",
    "dev": "npm run prepare:wasm && vite",
    "build": "npm run prepare:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.67"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.3.3",
    "vite": "^7.3.1"
  }
}
